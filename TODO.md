# TODO

Ενιαία λίστα εκκρεμοτήτων για τα 3 repos του project Γαλάτιστας
(`intake-tool`, `invoices`/invoicebook, `report-tool`) — το ίδιο αρχείο
διατηρείται και στα τρία, ώστε να είναι διαθέσιμο όποιο repo κι αν έχεις
ανοιχτό. Βλ. `DONE.md` για ό,τι έχει ήδη ολοκληρωθεί.

## [Cross-repo] Κλείσιμο κεφαλαίου intake-tool — έλεγχοι & βήματα απόσυρσης (ΞΕΚΙΝΑ ΕΔΩ, 2026-09-23)

Το invoicebook έχει πλέον 100% λειτουργική αντιστοιχία με το intake-tool (βλ. DONE.md,
«Ενσωμάτωση intake-tool → invoicebook»· επιβεβαιώθηκε 2026-09-23 με πλήρη σύγκριση cmd-names
των δύο `bridge.py` — τα μόνα 3 ονόματα που υπήρχαν μόνο στο intake-tool ήταν ψευδοθετικά:
`list_suppliers`≡`get_suppliers`, `get_settings`/`save_settings` νεκρός κώδικας χωρίς UI).
Επόμενο ερώτημα: πότε/πώς αποσύρεται πραγματικά το intake-tool. **Δεν έχει αποφασιστεί ακόμα
—μόνο σημειώνονται τα βήματα, καμία ενέργεια δεν έγινε.**

**Α) Έλεγχοι πριν αποφασιστεί οτιδήποτε:**
- [ ] Πραγματική καθημερινή χρήση του invoicebook (όχι μόνο δοκιμές σε throwaway αντίγραφα)
      για κάποιο διάστημα πριν αποσυρθεί το intake-tool — να φανεί στην πράξη ότι καλύπτει
      πλήρως τη ροή Εισαγωγή→Περιήγηση/Διόρθωση→Αποθέματα→Εκρηκτικά→Backup.
- [ ] Επιβεβαίωση ότι το πρώτο πραγματικό Gemini batch ιστορικών εκρηκτικών (ήδη ανοιχτό
      TODO item παρακάτω) θα γίνει μέσω invoicebook πλέον, όχι intake-tool — αλλαγή
      συνήθειας, όχι κώδικα, αλλά αξίζει ρητή απόφαση πριν ξεκινήσει το batch.

**Β) Πραγματικές εξαρτήσεις που ΘΑ χρειαστούν μετακόμιση αν αποσυρθεί το repo** (βρέθηκαν
με έλεγχο του `C:\intake-tool` — αυτά ΔΕΝ είναι λειτουργικότητα της ίδιας της εφαρμογής,
είναι υποδομή/εργαλεία που τυχαίνει να ζουν εκεί):
- [ ] `launch-portable.ps1` (ρίζα του intake-tool repo) — ο launcher και για τα 3
      προγράμματα (`-App intake-tool|invoicebook|report-tool`) στο two-machine setup. Θέλει
      νέο σπίτι (πιθανότατα το invoicebook repo, αφού γίνεται ο νέος «κύριος» κόμβος).
- [ ] `setup-computer.ps1` (ρίζα) — bootstrap ΝΕΟΥ υπολογιστή, γραμμένο με το intake-tool ως
      το κύριο/entry repo (κλωνοποιεί invoices ως «εξάρτηση» του). Χρειάζεται ξαναγράψιμο με
      το invoicebook ως το κύριο repo αν το intake-tool πάψει να είναι το entry point.
- [ ] `scripts/restore-backup-config-from-drive.ps1` — γίνεται περιττό μετά τη μετακόμιση
      του `backup_config.json` δίπλα στη βάση (ήδη σημειωμένο παλιότερα σε αυτό το αρχείο)
      — καθαρή κατάργηση, όχι μετακόμιση.
- [ ] `tools/pair_pdfs_by_date.py` (+ `pair_pdfs_by_order.py`/`pair_pdfs_with_json.py`) — το
      εργαλείο ζευγαρώματος PDF που χρησιμοποιείται ενεργά στη ροή εισαγωγής μέσω Gemini
      (βλ. μνήμη «Gemini batch import workflow»). Πραγματική καθημερινή εξάρτηση, ΟΧΙ
      προαιρετικό — πρέπει να μετακομίσει κάπου προσβάσιμο (πιθανότατα invoicebook's
      `tools/`) πριν αποσυρθεί το intake-tool repo.
- [ ] `domains/common.py` (43 γραμμές) — φαίνεται νεκρό κατάλοιπο από την παλιά
      fuel/repairs domain αρχιτεκτονική (καμία αναφορά βρέθηκε πουθενά αλλού). Πιθανώς απλή
      διαγραφή, όχι μετακόμιση — επιβεβαίωσε πριν το σβήσεις.

**Γ) Μετά την απόσυρση (αν/όταν αποφασιστεί):**
- [ ] Ενημέρωση CLAUDE.md σε όλα τα repos ώστε να μην αναφέρουν πια το intake-tool ως
      ζωντανό/ενεργό πρόγραμμα.
- [ ] Απόφαση: αρχειοθέτηση του `C:\intake-tool` repo (read-only, όχι διαγραφή — παραμένει
      ιστορικό υπόβαθρο/δίχτυ ασφαλείας) vs πλήρης διαγραφή. Καμία απόφαση ακόμα.
- [ ] Το ξεχωριστό ερώτημα «3 ξεχωριστά Electron windows» (section παρακάτω) ίσως αξίζει
      re-scope μετά την απόσυρση του intake-tool (θα μείνουν μόνο 2 παράθυρα: invoicebook +
      report-tool).

## [Cross-repo] Two-machine split (coding PC vs. data-entry PC) — ΚΛΕΙΣΤΟ (2026-09-24)

Ο διαχωρισμός ολοκληρώθηκε: το coding PC γυρίζει στον αρχικό του ρόλο (μόνο
development/testing, δική του τοπική/throwaway βάση, καμία σχέση πλέον με τον φορητό
δίσκο) — **δεν αποσύρεται**, απλά δεν κάνει πια πραγματική καταχώρηση. Το data-entry ρόλο
τον παίζει πλέον αυτό το (νέο) μηχάνημα, πάνω στον εξωτερικό δίσκο `INVOICEBOOK-DATA`
(`invoicebook.db` + `pdf_store` + `backup_config.json`, ανοίγει πάντα μέσω
`launch-portable.ps1`, ποτέ σκέτο `npm start`). Πλήρες ιστορικό της επαλήθευσης (ποια
βάση/backup-on-close/καθαρισμός) στο DONE.md, 2026-09-24.

Δύο σχεδόν άσχετα, ξεχωριστά product items έμειναν ανοιχτά από αυτό το κεφάλαιο (δεν
μπλοκάρουν τίποτα):
- [ ] Επιβεβαίωση ότι κλείσιμο με πραγματικά αργό πρώτο sync (πιθανόν αρκετά λεπτά) δεν
      μπερδεύει τον χρήστη — δεν υπάρχει κουμπί "ακύρωση" μέσα στην εφαρμογή, μόνο Task
      Manager αν χρειαστεί βίαιη διακοπή. Αν φανεί μπερδεμένο στην πράξη, ίσως χρειαστεί
      ένα ρητό μήνυμα "μπορεί να πάρει αρκετά λεπτά την πρώτη φορά" στο overlay.
- [ ] Ιδέα "heartbeat mid-session" (σταδιακό upload PDF κατά τη χρήση) **απορρίφθηκε
      προς το παρόν** (2026-09-21, σκεπτικό στο DONE.md) — ξανανοίγει ΜΟΝΟ αν σε "ζεστό"
      production προορισμό το backup-on-close συνεχίζει να είναι αργό στην πράξη.

## [Cross-repo] Ενσωμάτωση intake-tool → invoicebook — λειτουργικό κεφάλαιο ΚΛΕΙΣΤΟ (2026-09-23)

Ξεκίνησε 2026-09-19 ως ανά-feature μεταφορά (χωρίς επίσημο repo-level πλάνο απόσυρσης του
intake-tool) και ολοκληρώθηκε 2026-09-23, **πλέον 100%**: το invoicebook έχει πλήρη
λειτουργική αντιστοιχία με ΟΛΑ τα tabs του intake-tool, συμπεριλαμβανομένου του UI των
Ρυθμίσεων backup (Phase 2 πρόσθεσε και αυτό, `e755c11`). Πλήρες ιστορικό/σκεπτικό ανά κομμάτι
στο DONE.md — εδώ μόνο η λίστα:

Εισαγωγή/Συγχωνεύσεις (`08edc07`) → Αποθέματα (`05f3814`) → Περιήγηση Φάσεις 1-4 read-only/
review-toggle/edit-invoice/split-row (`9558039`/`aed05bb`/`a28f58b`/`22db94d`) → Εκρηκτικά→
expvault export (`3bafa7e`) → backup-on-close Phase 1 (`9afd7b4` invoicebook + `c0cc180`
intake-tool) → Ρυθμίσεις Phase 2, πλήρες backup config UI (`e755c11`).

Το intake-tool **δεν αποσύρεται** — συνεχίζει κανονικά όπως πάντα (ρητή απόφαση 2026-09-19).
Η σύνδεση report-tool↔invoicebook δεν επηρεάστηκε καθόλου.

**Ό,τι μένει (ξεχωριστό, όχι λείψανο αυτής της ενσωμάτωσης):**
- [ ] Το ξεχωριστό, μεγαλύτερο αρχιτεκτονικό ερώτημα «3 ξεχωριστά Electron windows»
      (παρακάτω section) — η σημερινή ενσωμάτωση ήταν σε επίπεδο λειτουργικότητας/κώδικα,
      όχι συγχώνευση παραθύρων/repos.
- [ ] Ιδέα (όχι αίτημα): αξίζει project skill `run-invoicebook` (μοτίβο ίδιο με το
      `run-intake-tool`) — οι δοκιμές όλων των παραπάνω φάσεων χρειάστηκαν custom one-off
      Playwright scripts αφού δεν υπήρχε ήδη ένα. Μόνο αν ζητηθεί.

## [intake-tool/expvault] Batch export τιμολογίων εκρηκτικών από invoicebook προς expvault — εξαγωγή ΥΛΟΠΟΙΗΘΗΚΕ 2026-09-23, μένει το πραγματικό batch

Η εξαγωγή JSON χτίστηκε (καρτέλα «💥 Εκρηκτικά → expvault», βλ. DONE.md) — και υπάρχει πλέον
ΚΑΙ στο invoicebook (ίδια καρτέλα, commit `3bafa7e`, 2026-09-23), χρησιμοποίησε όποιο από τα
δύο προγράμματα είναι ανοιχτό. Το παρακάτω κείμενο μένει ως τεκμηρίωση αποφάσεων. Ανοιχτά
βήματα (ισχύουν και για τα δύο προγράμματα — η ίδια βάση):
- [ ] Αποκλεισμός των 10 αρχείων του φακέλου σαρώσεων (λίστα παρακάτω, «Ενημέρωση 2026-09-18»).
- [ ] Πρώτο πραγματικό Gemini batch των ιστορικών εκρηκτικών — το prompt για «Άδεια: …,
      Εκδούσα αρχή: …» και «Σκοπός διακίνησης: …» δεν έχει δοκιμαστεί ακόμα σε πραγματικό run.
- [ ] Στην προεπισκόπηση της εξαγωγής: έλεγχος κατεύθυνσης στο PDF για τα 6 δελτία φύλαξης
      (`3.02/3.03/3.05/3.06/3.09/3.13`, ιδίως το `3.09`) και για τα `2.04/2.09/2.11`
      (ΣΙΘΩΝ→EXTRACO επιστροφές — ο «προμηθευτής» πρέπει να είναι η EXTRACO, όχι η ΣΙΘΩΝ).
- [ ] Εισαγωγή του αρχείου στο expvault: η «Εισαγωγή JSON/CSV» υπάρχει **μόνο στο ExpVault+
      (branch `v2`)**, όχι στο ExpVault 1.2.0 (`main`). Το πεδίο `paratirishis` («ΠΡΟΣ/ΑΠΟ
      ΦΥΛΑΞΗ» στο βιβλίο) περνάει μόνο μετά από νέα έκδοση του ExpVault+ (commit στο `v2`
      2026-09-23)· μέχρι τότε η εισαγωγή δουλεύει κανονικά, απλώς η παρατήρηση μένει η
      προεπιλεγμένη «Εισαγωγή από JSON/CSV».
- [ ] **ΙΔΕΑ χρήστη (2026-09-23), όχι σχεδιασμένη — αλληλοέλεγχος expvault ↔ invoicebook
      («απόδειξη εισαγωγής»)**. Σήμερα το ιστορικό εξαγωγών (`tbl_invoice_exports`) ξέρει μόνο
      ότι **φτιάχτηκε** αρχείο, όχι ότι **εισήχθη** στο expvault — ένα αρχείο που δεν
      εισήχθη ποτέ φαίνεται «✓ Εξήχθη». Ιδέα: το expvault, μετά την εισαγωγή, επιστρέφει
      ένα αρχείο που το invoicebook/intake-tool αναγνωρίζει ως εισαγωγή **συγκεκριμένου**
      export και σημειώνει τα παραστατικά ως «εισήχθη στο expvault» (όχι μόνο «εξήχθη»).
      Πρόχειρο σχήμα (για συζήτηση): κάθε export παίρνει μοναδικό αναγνωριστικό μέσα στο
      JSON· το ExpVault+ γράφει μετά την υποβολή ένα αρχείο-απόδειξη με αυτό το
      αναγνωριστικό + ποια παραστατικά καταχωρήθηκαν (και ποια όχι/με σφάλμα)· το
      intake-tool το φορτώνει και ενημερώνει το ιστορικό. Απαιτεί αλλαγή και στο expvault
      (`v2`). **Περιορισμός που σημείωσε ο χρήστης: όλα αυτά είναι PC-specific** — τα
      αρχεία export/απόδειξης ζουν στο μηχάνημα όπου τρέχουν και τα δύο προγράμματα (το PC
      καταχώρησης)· να ληφθεί υπόψη στο σχέδιο (π.χ. πού αποθηκεύεται η απόδειξη, τι γίνεται
      αν export και εισαγωγή γίνουν σε διαφορετικά μηχανήματα). Να οριστεί scope πριν από
      οποιαδήποτε υλοποίηση.


Νέος προμηθευτής εκρηκτικών ξεκινάει· δύο ξεχωριστές φάσεις υλικού:

1. **Ιστορικά τιμολόγια** — ήδη σκαναρισμένα ως εικόνα σε PDF, χωρίς πραγματικό text
   layer. Θα περάσουν από το intake-tool/Gemini (μόνο αυτό διαβάζει αξιόπιστα σκαναρισμένη
   εικόνα) → invoicebook, με `category="Εκρηκτικά"` σε επίπεδο γραμμής (ίδιο μοτίβο free-text
   category με όλες τις άλλες κατηγορίες).
2. **Τρέχοντα/μελλοντικά τιμολόγια** — πραγματικά PDF απευθείας από τον προμηθευτή, με
   πραγματικό text layer, ίδιας μορφής με αυτά που ήδη δέχεται το expvault σήμερα
   (EpsilonNet/NITROCHEM ή νέο πρότυπο). Αυτά **δεν χρειάζονται καθόλου** το bridge
   παρακάτω — μπαίνουν απευθείας και ανεξάρτητα και στα δύο apps, το καθένα διαβάζοντας το
   ίδιο πραγματικό αρχείο: expvault μέσω του ήδη υπάρχοντος "Εισαγωγή PDF" (αυτόματο
   άδεια/εκδούσα αρχή/ποσότητες μέσω δικού του regex parser, `pdf_parser.py`, εφόσον υπάρχει
   πρότυπο για τον προμηθευτή), intake-tool μέσω Gemini όπως κάθε άλλο τιμολόγιο. Αν κάποια
   στιγμή το expvault αποτύχει να αναγνωρίσει τη μορφή ενός νέου προμηθευτή, το bridge
   παρακάτω λειτουργεί και ως fallback.

**Αρχιτεκτονική απόφαση (σκεπτικό, όχι μόνο προτίμηση)**: το bridge πηγαίνει **intake-tool →
JSON export → expvault import**, ΟΧΙ αντίστροφα (εμπλουτισμός expvault με τιμές + export προς
intake-tool). Λόγος: θα ξανάχτιζε μέσα στο expvault ό,τι ήδη υπάρχει στο invoicebook
(unit_price/value/ΦΠΑ, στοιχεία πελάτη/προμηθευτή ως λογιστική οντότητα) — ακριβώς το ίδιο
λάθος που διορθώθηκε τον Αύγουστο 2026 όταν καταργήθηκαν τα ξεχωριστά per-domain modules
(`domains/fuel/`, `domains/repairs/`, βλ. `CLAUDE.md`'s Ιστορικό) υπέρ ενός ενιαίου core. Το
expvault είναι σκόπιμα στενό (νόμιμο βιβλίο ποσοτήτων, όχι λογιστικό εργαλείο) — οι κινήσεις
**ΚΑΤΑΝΑΛΩΣΗΣ** μάλιστα δεν έχουν καν τιμολόγιο από πίσω (εσωτερική χρήση), άρα δεν θα
χωρούσαν καν σε ένα "invoice" σχήμα προς εξαγωγή στο intake-tool.

**Σχεδιασμένο ως μόνιμο feature στο intake-tool** (επιλογή τιμολογίων/κατηγορίας/εύρους
ημερομηνιών → κουμπί "Εξαγωγή JSON για expvault" → αρχείο), ΟΧΙ σκόρπιο one-off script —
θα χρησιμοποιείται επαναλαμβανόμενα όσο προχωράει το ιστορικό backlog, και παραμένει
χρήσιμο ως fallback στη Φάση 2.

### Πλήρης καταγραφή — τι είναι κλειδωμένο, τι μένει ανοιχτό (ενημερώθηκε 2026-09-22)

Σκοπός αυτής της λίστας: όταν κάποιος (εγώ σε επόμενη session, ή ο χρήστης) γυρίσει σε αυτό
το feature, να μη χρειαστεί να ξαναδιαβάσει όλη την αφήγηση παρακάτω για να καταλάβει πού
έχουμε μείνει. Οι λεπτομέρειες/το σκεπτικό κάθε σημείου είναι στην αναλυτική αφήγηση πιο κάτω
σε αυτό το ίδιο section.

**Κλειδωμένες αποφάσεις (δεν χρειάζονται άλλη συζήτηση για να ξεκινήσει η υλοποίηση):**
- [x] Κατεύθυνση bridge: intake-tool → export JSON → expvault import. Ποτέ αντίστροφα.
- [x] Μόνιμο feature μέσα στο intake-tool UI (όχι one-off script): επιλογή τιμολογίων/
      κατηγορίας/εύρους ημερομηνιών → κουμπί "Εξαγωγή JSON για expvault".
- [x] Μόνο γραμμές με `category="Εκρηκτικά"` περνάνε στο export, **ανά γραμμή**, όχι όλο το
      τιμολόγιο μαζί όταν έχει μεικτές κατηγορίες.
- [x] Αποκλείονται από το export: γραμμές εξόδων/μεταφορικών/περιβαλλοντικής εισφοράς, και
      οτιδήποτε περάσει από το bulk-pool/machine-allocation μηχανισμό του invoicebook
      (`tbl_bulk_pools` — άσχετη έννοια με το νόμιμο υπόλοιπο άδειας του expvault).
- [x] `doc_type`→`tipos`: "Τιμολόγιο Πώλησης"→`ΕΙΣΑΓΩΓΗ`, "Πιστωτικό Τιμολόγιο"→`ΕΠΙΣΤΡΟΦΗ`
      (επιβεβαιωμένο στο πραγματικό expvault schema, όχι υπόθεση).
- [x] `unit_price`/`value`/`vat_pct`: ποτέ δεν περνάνε — το `kiniseis` table δεν έχει καν
      στήλη τιμής/αξίας.
- [x] `unit`: καμία μετατροπή ανά κίνηση· αρκεί σωστό ταίριασμα `yliko_id`, η μονάδα
      ακολουθεί από το ίδιο το υλικό (`ylika.monada_metrisis`).
- [x] `nomiki_katigoria`: αυτόματη μέσω σωστού `yliko_id` match — το intake-tool δεν
      αναπαράγει καθόλου τη λογική ταξινόμησης του expvault's `κατηγοριες_εκρηκτικων.txt`.
- [x] `adeia`/`ekdousa_archi`: δεν γίνονται νέο πεδίο στο invoicebook σχήμα — ήδη
      υλοποιημένο (2026-09-17) ως αυτόματη εξαγωγή αριθμού άδειας/εκδούσας αρχής μέσα στο
      invoice-level `notes` (μορφή "Άδεια: ####, Εκδούσα αρχή: …") μέσω του `IMPORT_PROMPT`.
- [x] `agora_ref`: μένει **κενό** στο export· λύνεται από το ήδη υπάρχον interactive βήμα
      μέσα στο expvault ("Βήμα 2: Συσχέτισε με τιμολόγιο αγοράς"). Το batch JSON πρέπει να
      ταξινομείται κατά `doc_date` αύξουσα ώστε κάθε αγορά να μπαίνει πριν την επιστροφή της.
- [x] Φύλαξη: ΛΑΤΟΜΕΙΟ→ΦΥΛΑΞΗ καταχωρείται σαν `ΕΠΙΣΤΡΟΦΗ`, ΦΥΛΑΞΗ→ΛΑΤΟΜΕΙΟ σαν `ΕΙΣΑΓΩΓΗ`
      χωρίς τιμή· η οντότητα φύλαξης μπαίνει σαν `promitheftis`. Fallback: αν φανεί
      προβληματικό στην πράξη, σταματάμε και περιμένουμε πραγματικό tipos ΦΥΛΑΞΗ στο
      expvault αντί να αλλάξουμε άλλο πράγμα.
- [x] Ταίριασμα `yliko_id`/`promitheftis_id`/`adeia_id`: αν δεν βρεθεί υπάρχουσα εγγραφή
      expvault, **αυτόματη δημιουργία νέας** — όχι block-and-wait.

**Ανοιχτά/εκκρεμή πριν αρχίσει πραγματικός κώδικας:**
- [ ] Ίδιο το export feature (backend command στο `bridge.py` + UI tab/κουμπί) — καθόλου
      κώδικας ακόμα, μόνο σχέδιο.
- [ ] Import-side στο expvault: πώς ακριβώς θα δέχεται αυτό το JSON (νέο bridge command εκεί;
      χειροκίνητο file import μέσω υπάρχοντος μηχανισμού;) — δεν έχει διερευνηθεί καθόλου
      ακόμα, μόνο το schema/domain model διαβάστηκε.
- [ ] Επιβεβαίωση σε πραγματικά δεδομένα ότι `promitheftis_id`/`adeia_id` δέχονται εγγραφές
      χωρίς πραγματικό λογιστικό υπόβαθρο (οι κινήσεις ΦΥΛΑΞΗ→ΕΙΣΑΓΩΓΗ δεν έχουν πραγματικό
      "πωλητή" πίσω τους) — δεν ελέγχθηκε αν υπάρχουν επιπλέον NOT NULL/business-logic
      περιορισμοί πέρα από το DDL.
- [ ] Δοκιμή του `adeia`/`ekdousa_archi` notes-extraction σε πραγματικό Gemini run — μέχρι
      τώρα επιβεβαιωμένο μόνο manually πάνω σε 10 δείγματα, ποτέ μέσω πραγματικού AI import.
- [ ] Φυσικός αποκλεισμός/μετακίνηση των ήδη-εντοπισμένων duplicate/μη-παραστατικών PDF από
      τον φάκελο σαρώσεων (review 2026-09-18, 10 αρχεία: `1.03/1.05/1.06/1.08/1.12/1.16/
      1.17/1.18/1.19/3.08`) — έχει γίνει μόνο ανάλυση, όχι φυσική μετακίνηση/αποκλεισμός.
- [ ] Ταξινόμηση/επιβεβαίωση κατεύθυνσης (προς/από φύλαξη) για κάθε ένα από τα 6
      ΦΥΛΑΞΗ-αρχεία (`3.02/3.03/3.05/3.06/3.09/3.13`) πριν μπουν στο πρώτο πραγματικό batch,
      ώστε να εφαρμοστεί η σωστή κατεύθυνση `ΕΠΙΣΤΡΟΦΗ`/`ΕΙΣΑΓΩΓΗ`.

**Field mapping** (invoicebook `tbl_invoices`/`tbl_invoice_items` → expvault JSON schema,
βλ. `expvault/backend/import_data.py`'s `_build_suggested()`):
- `doc_date`→`imerominia`, `doc_number`→`arithmos_parstatikou`, supplier name→`promitheftis`,
  `description`→`onoma`, `quantity`→`posotita` — 1:1.
- `doc_type`→`tipos`: **επιβεβαιώθηκε 2026-09-22** πάνω στο πραγματικό expvault repo
  (README.md + `backend/database.py`'s `kiniseis.tipos` CHECK constraint) — "Τιμολόγιο
  Πώλησης"→ΕΙΣΑΓΩΓΗ, "Πιστωτικό Τιμολόγιο"→ΕΠΙΣΤΡΟΦΗ. Το `tipos` είναι hard CHECK, μόνο
  4 τιμές επιτρέπονται: `ΕΙΣΑΓΩΓΗ`/`ΚΑΤΑΝΑΛΩΣΗ`/`ΕΠΙΣΤΡΟΦΗ`/`ΕΞΑΓΩΓΗ` — καμία άλλη τιμή δεν
  γίνεται δεκτή στη βάση.
- `unit_price`/`value`/`vat_pct` → **δεν περνάνε καθόλου** — επιβεβαιώθηκε ότι δεν είναι απλώς
  "δεν χρειάζεται", το `kiniseis` table **δεν έχει καν στήλη τιμής/αξίας** στο schema.
- `unit`→`monada`: **αναθεωρήθηκε 2026-09-22** — δεν είναι πεδίο ανά κίνηση προς
  normalization. Η μονάδα ζει στο ίδιο το υλικό (`ylika.monada_metrisis`, σταθερή ανά
  `(onoma, diatomi_mm)`), όχι στο `kiniseis`. Άρα δεν χρειάζεται μετατροπή μονάδας στο export
  — χρειάζεται **σωστό ταίριασμα `yliko_id`** στο ήδη υπάρχον υλικό (βλ. πολιτική
  ταιριάσματος/auto-create παρακάτω), η μονάδα ακολουθεί αυτόματα.
- `nomiki_katigoria` (η νομική κατηγορία που παρακολουθούν οι άδειες) **ζει ήδη στο `ylika`**,
  όχι ανά κίνηση/γραμμή — άρα μόλις ταιριάξει σωστά το `yliko_id`, η κατηγοριοποίηση έρχεται
  αυτόματα. Το intake-tool **δεν χρειάζεται** να αναπαράγει τη λογική ταξινόμησης του
  expvault's `κατηγοριες_εκρηκτικων.txt` (κωδικοί 400/443 κλπ, λέξεις-κλειδιά "AN-FO"/
  "POLADYN") — αυτό μένει αποκλειστικά δουλειά του expvault.
- `adeia`/`ekdousa_archi`: **δεν υπάρχουν καθόλου στο invoicebook σχήμα** (σκόπιμα — ειδικά
  μόνο για εκρηκτικά, αντίθετα στον κανόνα "γενικό schema" του intake-tool). **Υλοποιήθηκε
  2026-09-17** (`js/import.js`'s `IMPORT_PROMPT`, πεδίο `notes`, όχι νέο dedicated πεδίο):
  όταν category="Εκρηκτικά" σε οποιαδήποτε γραμμή, ζητάει από το Gemini να ψάξει
  "ΑΡΙΘ. ΑΔΕΙΑΣ"/"ΕΚΔΟΥΣΑ ΑΡΧΗ" (τυπωμένα ή χειρόγραφα "ΑΔΕΙΑ ####"/"Α.Δ. <περιοχή>") και να
  τα βάλει ως πρώτη γραμμή στο invoice-level `notes` σε μορφή "Άδεια: <αριθμός>, Εκδούσα
  αρχή: <αρχή>" — ελεύθερο κείμενο, όχι στήλη βάσης, ώστε να διαβάζεται αργότερα (ίδιο
  πνεύμα με το expvault's regex parsing). Επιβεβαιώθηκε σε 10 πραγματικά ιστορικά τιμολόγια
  εκρηκτικών (NITROCHEM Ε.Π.Ε., 2012) πριν την υλοποίηση — συνεπές `ΑΔΕΙΑ 40098`/`Α.Δ.
  ΧΑΛΚΙΔΙΚΗΣ` σε όλα. **Δεν έχει δοκιμαστεί ακόμα σε πραγματικό Gemini run.**
  - **Σημαντικό εύρημα από τα ίδια δείγματα**: περίπου 40% των αρχείων (4/10) ήταν
    "Έγγραφο Μεταφοράς" — ξεχωριστό ADR/κανονιστικό έγγραφο που απλώς επαναλαμβάνει τα ίδια
    είδη/ποσότητες του αντίστοιχου ΔΕΛΤΙΟΥ ΑΠΟΣΤΟΛΗΣ (ίδιο "Σχετικό παραστατικό" αριθμό),
    χωρίς καμία νέα πληροφορία, μερικές φορές και σε πολλαπλά αντίγραφα του ίδιου. **Αυτά
    πρέπει να αποκλείονται χειροκίνητα από το χρήστη πριν το batch** (όχι κάτι που λύνεται
    στο prompt) — αλλιώς δημιουργούν πλήρη διπλότυπα με μηδενική αξία στη βάση. Καμία τιμή/
    αξία δεν βρέθηκε σε κανένα από τα 10 δείγματα (ούτε στα ΔΕΛΤΙΑ ΑΠΟΣΤΟΛΗΣ) — αναμενόμενο
    για όλο το ιστορικό batch.

**Ενημέρωση 2026-09-18 — πλήρες review του φακέλου σαρώσεων** (`C:\Users\papadcha\Desktop\
New folder\`, 44 PDF):
- Ο φάκελος δεν είναι μόνο NITROCHEM όπως υποτέθηκε — 3 ξεχωριστοί προμηθευτές: σειρά
  `1.xx` (ανώνυμος εκδότης, 2012, ΑΔΕΙΑ 40098/Α.Δ. ΧΑΛΚΙΔΙΚΗΣ), `2.xx` (EXTRACO A.E.,
  2011-2012), `3.xx` (VI.EK.AL Α.Ε., 2011).
- 10 αρχεία επιβεβαιώθηκαν προς αποκλεισμό πριν το batch: `1.03/1.05/1.06/1.08/1.12`
  (Έγγραφο Μεταφοράς duplicates αντίστοιχα των 1.02/1.04(×2)/1.07/1.11), `1.16/1.17/1.18/
  1.19` (σαρώσεις της ίδιας ΑΔΕΙΑΣ 40098/περιοριστικού όρου, όχι καν παραστατικά — 2
  αντίγραφα η καθεμία), `3.08` (literal duplicate scan του 3.07). **Δεν έχουν ακόμα
  μετακινηθεί/αποκλειστεί φυσικά — μόνο ανάλυση.**
- **Νέο θέμα, εκτός αρχικού πλάνου**: αρκετά αρχεία είναι ΕΠΙΣΤΡΟΦΕΣ ή ΦΥΛΑΞΗ, όχι απλές
  αγορές — `2.04/2.09/2.11` (ΣΙΘΩΝ→EXTRACO, ΕΠΙΣΤΡΟΦΗ), `3.02/3.03/3.05/3.06/3.09/3.13`
  (ΔΕΛΤΙΟ ΑΠΟΣΤΟΛΗΣ ΕΜΠΟΡΕΥΜΑΤΩΝ ΑΠΟ/ΠΡΟΣ ΦΥΛΑΞΗ).
  **ΑΠΟΦΑΣΗ ΦΥΛΑΞΗΣ (επιβεβαιώθηκε ξανά από τον χρήστη 2026-09-23 — ΜΗΝ ξανανοίξει)**:
  **ΑΠΟ ΦΥΛΑΞΗ → ΕΙΣΑΓΩΓΗ**, **ΠΡΟΣ ΦΥΛΑΞΗ → ΕΠΙΣΤΡΟΦΗ** στο JSON export. Σκεπτικό: στο
  expvault χρήση άδειας = ΕΙΣΑΓΩΓΕΣ − ΕΠΙΣΤΡΟΦΕΣ, οπότε ένας πλήρης κύκλος φύλαξης
  αλληλοαναιρείται (καθαρό 0 στην άδεια), ενώ το υπόλοιπο δείχνει πάντα τη φυσική κατοχή.
  **ΟΧΙ ΕΞΑΓΩΓΗ** (ο expvault τύπος ΕΞΑΓΩΓΗ δεν μετράει στην άδεια και τυπώνεται ως
  ΕΠΙΣΤΡΟΦΗ — 0 χρήσεις στην πραγματική βάση). Στην υλοποίηση: (α) κατεύθυνση από
  αποστολέα/παραλήπτη του εγγράφου, όχι μόνο από τη φράση — το `3.09` γράφει «ΑΠΟ ΦΥΛΑΞΗ»
  αλλά είχε σημειωθεί και «ΕΠΙΣΤΡΟΦΗ», έλεγχος PDF για καθένα από τα 6· (β) στις
  παρατηρήσεις της κίνησης «ΠΡΟΣ ΦΥΛΑΞΗ»/«ΑΠΟ ΦΥΛΑΞΗ», ώστε να φαίνεται στο βιβλίο· (γ) η
  ΠΡΟΣ ΦΥΛΑΞΗ ως ΕΠΙΣΤΡΟΦΗ θέλει `agora_ref` όπως κάθε επιστροφή (ίδιο interactive βήμα
  του expvault, βλ. «Επιστροφές» παρακάτω).
- **Χειροκίνητο crosscheck ΟΛΟΚΛΗΡΩΘΗΚΕ 2026-09-19** — άνοιγμα και των 4 PDF:
  `1.10` (NitroChem, ΔΑΑ00000007, 10/08/2012) επιβεβαιώθηκε πλήρες αυτοτελές έγγραφο,
  απλώς χωρίς ζευγαρωμένο Έγγραφο Μεταφοράς στο set — καμία ένδειξη προβλήματος,
  ασφαλές για το batch όπως είναι. `3.09/3.10/3.11` (VI.EK.AL, ίδια ημερομηνία
  6/10/2011) επιβεβαιώθηκαν ως 3 γνήσιες ξεχωριστές αποστολές, όχι διπλότυπα:
  διαφορετική σειρά (`3.09`=Σειρά Γ Νο 1274, "ΑΠΟ ΦΥΛΑΞΗ"/ΕΠΙΣΤΡΟΦΗ· `3.10`/`3.11`=
  Σειρά Β, διαδοχικοί αριθμοί 1975/1976, ΠΩΛΗΣΗ), τελείως διαφορετικά
  είδη/ποσότητες σε κάθε ένα.

**Επιστροφές (πιστωτικά) — ειδικό θέμα, μην το αγνοήσεις όταν χτιστεί**:
- Το expvault θέλει `agora_ref` (ποιο αρχικό τιμολόγιο αγοράς αφορά η επιστροφή) — το
  invoicebook δεν έχει καμία έννοια invoice-to-invoice link σήμερα. **v1: μην το λύσεις στο
  intake-tool** — άσε το `agora_ref` κενό στο export, το expvault's ήδη υπάρχον interactive
  flow ("Βήμα 2: Συσχέτισε με τιμολόγιο αγοράς", `js/pdf-import.js`'s `submitPdfEntries()`)
  το καλύπτει ήδη μετά το submit, ίδιο μηχανισμό με τα σημερινά χειροκίνητα PDF imports.
  **Επιβεβαιώθηκε ξανά 2026-09-22** (ο χρήστης το επέλεξε ρητά ξανά μετά την εξήγηση ότι το
  `agora_ref` είναι πραγματικό TEXT πεδίο πάνω στο `kiniseis` — όχι μόνο UI βήμα — άρα θα
  μπορούσε τεχνικά να γεμίζει αυτόματα, αλλά αποφασίστηκε να παραμείνει στο expvault).
- **Σειρά εισαγωγής έχει σημασία**: το batch JSON πρέπει να ταξινομείται κατά `doc_date`
  αύξουσα ώστε η αγορά (ΕΙΣΑΓΩΓΗ) να μπαίνει στο expvault πριν από την αντίστοιχη επιστροφή
  της (ΕΠΙΣΤΡΟΦΗ) — το interactive linking flow υποθέτει ότι η αγορά υπάρχει ήδη.
- Μελλοντική βελτίωση (όχι v1): αν το intake-tool κάποτε αποκτήσει γενικό invoice-to-invoice
  "αφορά το τιμολόγιο X" link (θα ωφελούσε και επιστροφές λιπαντικών/άλλων κατηγοριών, άρα θα
  περνούσε τον κανόνα "γενικό schema"), τότε το export θα μπορούσε να συμπληρώνει το
  `agora_ref` αυτόματα αντί να βασίζεται στο interactive βήμα του expvault.

**Φύλαξη — απόφαση μοντελοποίησης (2026-09-22)**:
- Επιβεβαιώθηκε στο πραγματικό expvault repo (README.md + `kiniseis.tipos` CHECK constraint)
  ότι **δεν υπάρχει dedicated τύπος/workflow ΦΥΛΑΞΗ** — μόνο 4 τιμές επιτρέπονται:
  `ΕΙΣΑΓΩΓΗ`/`ΚΑΤΑΝΑΛΩΣΗ`/`ΕΠΙΣΤΡΟΦΗ`/`ΕΞΑΓΩΓΗ`. Τα ιστορικά `ΔΕΛΤΙΟ ΑΠΟΣΤΟΛΗΣ ΕΜΠΟΡΕΥΜΑΤΩΝ
  ΑΠΟ/ΠΡΟΣ ΦΥΛΑΞΗ` (3.02/3.03/3.05/3.06/3.09/3.13, βλ. review 2026-09-18 πιο πάνω) δεν έχουν
  πού να μπουν όπως είναι το schema σήμερα.
- **Απόφαση χρήστη** (προϋπάρχουσα συζήτηση, επιβεβαιώθηκε ξανά εδώ):
  - ΛΑΤΟΜΕΙΟ → ΦΥΛΑΞΗ (αποστολή προς φύλαξη): καταχωρείται στο expvault ως **`ΕΠΙΣΤΡΟΦΗ`**
    (ίδια κατεύθυνση με πραγματική επιστροφή — μειώνει το υπόλοιπο άδειας του λατομείου).
  - ΦΥΛΑΞΗ → ΛΑΤΟΜΕΙΟ (επιστροφή από φύλαξη): καταχωρείται ως **`ΕΙΣΑΓΩΓΗ`**, χωρίς τιμή —
    συμβατό με το ότι το `kiniseis` schema δεν έχει καν στήλη τιμής/αξίας (βλ. πιο πάνω), άρα
    δεν χρειάζεται καν ειδική μεταχείριση για το "χωρίς τιμή" μέρος.
  - **Διορθώθηκε 2026-09-22** (πραγματικό παράδειγμα: 3.05.pdf, ΔΕΛΤΙΟ ΑΠΟΣΤΟΛΗΣ που
    εξέδωσε η ίδια η ΣΙΘΩΝ Α.Ε., "ΣΚΟΠΟΣ ΔΙΑΚΙΝΗΣΗΣ: ΠΡΟΣ ΦΥΛΑΞΗ", προορισμός VI.EK.AL) —
    ο χρήστης επιβεβαίωσε την ήδη καθιερωμένη σύμβαση από πριν την ενοποίηση, όσο υπήρχε
    μόνο το expvault: **για κινήσεις ΛΑΤΟΜΕΙΟ→ΦΥΛΑΞΗ (δηλ. όταν το ίδιο το λατομείο εκδίδει
    το Δελτίο Αποστολής), `promitheftis` = το ΛΑΤΟΜΕΙΟ (ΣΙΘΩΝ Α.Ε.) — ο εκδότης του
    εντύπου — ΟΧΙ ο χώρος φύλαξης (VI.EK.AL).** Αντίστροφο από ό,τι θα υπέθετε κανείς
    διαισθητικά (ότι `promitheftis` = "σε ποιον επιστρέφεται/παραδίδεται το υλικό"). Για
    κινήσεις ΦΥΛΑΞΗ→ΛΑΤΟΜΕΙΟ (Δελτίο που εκδίδει η VI.EK.AL, "ΑΠΟ ΦΥΛΑΞΗ" — π.χ. 3.02/3.03/
    3.06/3.09.pdf) ισχύει το αντίθετο μοτίβο, ίδιο με τα κανονικά έγγραφα πώλησης:
    `promitheftis` = VI.EK.AL (πάλι ο εκδότης του εντύπου). Δηλαδή ο γενικός κανόνας είναι
    **`promitheftis` = όποιος εξέδωσε το συγκεκριμένο έντυπο**, όχι κάποιος σταθερός ρόλος
    ανά κατεύθυνση — αυτόματη δημιουργία αν δεν υπάρχει ήδη σαν προμηθευτής (βλ. πολιτική
    auto-create παρακάτω).
  - **Fallback αν φανεί προβληματικό στην πράξη** (π.χ. αλλοιώνει νόημα στο επίσημο 2σέλιδο
    PDF όπου θα εμφανίζονται ΕΠΙΣΤΡΟΦΗ/ΕΙΣΑΓΩΓΗ κινήσεις χωρίς πραγματικό
    αγοραστή/προμηθευτή πίσω τους): **σταματάμε και περιμένουμε πραγματικό dedicated tipos
    ΦΥΛΑΞΗ στο expvault** (πιθανό μελλοντικό feature στο branch `v2`) αντί να συνεχίσουμε να
    το κουτσουρεύουμε πάνω σε άσχετους τύπους κίνησης. Ο χρήστης ζήτησε ρητά να μην
    προχωρήσουμε σε αλλαγή schema expvault μόνο γι' αυτό, εκτός αν χρειαστεί.

**Πολιτική ταιριάσματος/auto-create για `yliko_id`/`promitheftis_id`/`adeia_id` (2026-09-22)**:
- Όταν το export βρίσκει προμηθευτή/υλικό/άδεια στο invoicebook που δεν ταιριάζει με καμία
  υπάρχουσα εγγραφή expvault: **αυτόματη δημιουργία νέας εγγραφής** (`promitheftes`/`ylika`/
  `adeies`), όχι block-and-wait-for-manual-reconciliation.
- Ο χρήστης εκτιμά ότι στα υλικά (`ylika`) αυτό ουσιαστικά δεν θα προκύψει ποτέ στην πράξη —
  ο κατάλογος υλικών/νομικών κατηγοριών θεωρείται ήδη πλήρης — αλλά η πολιτική ισχύει γενικά
  σε όλους τους τύπους, όχι μόνο σε προμηθευτές/άδειες. Αν προκύψει πρόβλημα ταιριάσματος σε
  υλικό στην πράξη, το ξανακοιτάμε τότε (δεν προσχεδιάζουμε λύση τώρα).

**Μεικτές κατηγορίες/γραμμές που ΔΕΝ πρέπει να περάσουν στο expvault**:
- Ένα τιμολόγιο μπορεί να έχει γραμμές πολλών κατηγοριών μαζί (πάγιο μοτίβο του intake-tool)
  — το export πρέπει να παίρνει **μόνο** τις γραμμές με `category="Εκρηκτικά"` ανά τιμολόγιο,
  όχι όλο το τιμολόγιο.
- Γραμμές τύπου έξοδα/μεταφορικά/περιβαλλοντική εισφορά (ίδιο μοτίβο με ό,τι βρέθηκε στο
  Λιπαντικά cleanup) δεν είναι πραγματικό υλικό — απέκλεισέ τις από το `grammes[]`.
- **Μην περάσεις γραμμές εκρηκτικών από το bulk-pool/machine-allocation μηχανισμό του
  invoicebook** (`tbl_bulk_pools`, χρησιμοποιείται σήμερα για καύσιμα/λιπαντικά) — είναι
  εννοιολογικά διαφορετικό πράγμα από την κατανάλωση που παρακολουθεί το expvault
  (κατανομή κόστους ανά μηχάνημα vs. νόμιμο υπόλοιπο άδειας). Μην τα μπερδέψεις κατά την
  καταχώρηση στο intake-tool· οι γραμμές εκρηκτικών μάλλον δεν χρειάζονται καθόλου
  `machine_id`/`bulk` flag.



Χρήστης παρατήρησε ότι το κλείσιμο της εφαρμογής αργεί — **πλήρως λυμένο, βλ.
DONE.md για όλη την ιστορία/διάγνωση/διορθώσεις (staleness-gate, reachability
pre-checks, GDrive→pCloud swap)**. Ένα μόνο υπόλοιπο, μη επείγον:
- [ ] Μεγαλύτερης εμβέλειας/μη προτεινόμενο άμεσα: παραλληλισμός των 3
      προορισμών αντί για σειριακό τρέξιμο — μειώνει τον συνολικό χρόνο αλλά
      προσθέτει πολυπλοκότητα (progress UI, ταυτόχρονα rclone processes).
      **Μετρήσεις 2026-09-23** (coding PC, dev προορισμοί): ανά προορισμό έλεγχος
      ~1–3″, σύγκριση 2713 PDF ~4″, ανέβασμα βάσης ήταν ο κύριος χρόνος — Mega ασταθές
      (8,4 MB: 10–56″). Μετά τη συμπίεση (βλ. DONE.md) ~5–9″ ανά προορισμό συνολικά.
      Ξανακοίτα το αν στο PC καταχώρησης (3 προορισμοί, NAS) το κλείσιμο μείνει αργό.

## [intake-tool] IMPORT_PROMPT: χειρόγραφα machine_name — ό,τι μένει ανοιχτό

Δοκιμάστηκε (2026-09-11) και ενσωματώθηκε μόνιμα (2026-09-13, βλ. DONE.md) ένα tweak
στη γραμμή `machine_name` του `IMPORT_PROMPT` (`js/import.js`) που ζητάει ρητά από το
Gemini να ψάξει και χειρόγραφες σημειώσεις/σκαλίσματα, όχι μόνο τυπωμένα πεδία. Πλήρης
ανάλυση: [[project-galatista-machine-name-handwriting-extraction]] (memory αρχείο, όχι
σε αυτό το repo).

- [ ] Μην προσπαθήσεις να λύσεις τις μακρινές/κάτω-στη-σελίδα σημειώσεις με άλλο prompt
      tuning — ρητή απόφαση χρήστη να μην συνεχίσουμε εκεί, αποδεκτό ως χειροκίνητο
      PDF spot-check μετά από κάθε import.
- [ ] Πρόσθεσε τον διπλότυπο μηχάνημα-τύπο σφάλμα (π.χ. "ΕΓΚΑΤΑΣΤΑΘΕΙΣ" αντί
      "ΕΓΚΑΤΑΣΤΑΣΕΙΣ", γραμματικό OCR-like typo του ίδιου του Gemini, όχι formatting) στη
      λίστα πραγμάτων να προσέχουμε στο επόμενο machine dedup sweep — το tier-1 automated
      dedup δεν το πιάνει (αλλάζει γράμμα, όχι μορφοποίηση).
- [ ] **Spot-check εκκρεμεί** (βλ. DONE.md "wrong-field-bleed bug διορθώθηκε",
      2026-09-16): δεν έχει δοκιμαστεί ακόμα σε νέο πραγματικό import batch ότι η
      διόρθωση πιάνει το bug χωρίς να χαλάσει σωστές γενικές τιμές (ΕΓΚΑΤΑΣΤΑΣΕΙΣ/
      ΣΥΓΚΡΟΤΗΜΑ/TOYOTA).
- [ ] Ίδιο batch (2026-09-15) επιβεβαίωσε ξανά ορθογραφική ασυνέπεια για το ΙΔΙΟ
      μηχάνημα — `CAT 980`/`980C`/`980`, 4 γραφές του "πλυντήριο"
      (`πληντυριο`/`ΠΛΥΝΤΗΡΙΟ`/`Πλωντύρω`/`ΟΛΗΝΤΥΡΙΟ`, μία φορά μέσα στο ΙΔΙΟ
      τιμολόγιο), `WD712`/`WP712` — θέμα για το επόμενο machine dedup sweep (βλ.
      ενότητα Μηχανήματα παρακάτω), όχι για το prompt.
- [ ] **Νέος τύπος σφάλματος εντοπίστηκε 2026-09-17 — fabricated machine_name
      (όχι misread/μακρινή σημείωση)**: invoice id 1722 (ΗΝΙΟΧΟΣ ΔΙΟΙΚΗΤΗΡΙΟΥ
      Α.Ε., Α-ΤΑΜ00070745, 27/12/2021 — ανταλλακτικά ανάρτησης/τιμονιού, μάλλον
      οχήματος όχι μηχανήματος λατομείου) είχε και στις 10 γραμμές του
      `machine_id` → "ΚΟΑΕΒ" (μηχάνημα id 190), ενώ το πραγματικό PDF δεν
      περιέχει ΠΟΥΘΕΝΑ (ούτε τυπωμένο, ούτε χειρόγραφο) καμία λέξη/κωδικό που να
      μοιάζει με αυτό — το Gemini το επινόησε εντελώς. Διορθώθηκε χειροκίνητα
      (τα 10 `machine_id`→NULL, backup
      `invoices/backend/invoicebook.db.bak-koaeb-cleanup-20260917_154613`, το
      πλέον orphan machine row 190 διαγράφηκε). **Ενημέρωση 2026-09-17, μετά το
      πλήρες sweep (βλ. DONE.md)**: σαφώς ΔΕΝ ήταν μεμονωμένο — από τα 42
      usage=1 μηχανήματα που ελέγχθηκαν σε επίπεδο PDF, **17 (~40%) ήταν
      field-bleed/fabrication λάθη**, όχι πραγματικά χειρόγραφα. Ξεχωρίζει ένα
      συγκεκριμένο, κυρίαρχο υπο-μοτίβο: σε τιμολόγια φίλτρων, το Gemini πιάνει
      το "συμβατό με" brand name της περιγραφής προϊόντος (π.χ. "Φ.ΑΕΡΟΣ
      M.FERGUSON, IHC") ή τον κωδικό προϊόντος (στήλη ΚΩΔΙΚΟΣ, π.χ. "MD260K")
      αντί για το πραγματικό χειρόγραφο σημείωμα δίπλα στη γραμμή — 4 ξεχωριστές
      τέτοιες περιπτώσεις κατέληξαν όλες στο ΙΔΙΟ πραγματικό μηχάνημα (`WD712`).
      **Prompt tweak υλοποιήθηκε 2026-09-17** (`js/import.js`'s `IMPORT_PROMPT`,
      γραμμή `machine_name`): προστέθηκαν 4 ρητές παγίδες προς αποφυγή —
      (1) "συμβατό με" brand names σε περιγραφές φίλτρων/ανταλλακτικών,
      (2) κωδικός προϊόντος από τη στήλη ΚΩΔΙΚΟΣ, (3) τυπωμένα πεδία
      ΟΧΗΜΑ/ΑΡΙΘΜΟΣ ΟΧΗΜΑΤΟΣ/ΜΕΤΑΦΟΡΙΚΟ ΜΕΣΟ/ΑΡ. ΑΥΤΟΚΙΝΗΤΟΥ (συνήθως όχημα
      παράδοσης προμηθευτή, όχι μηχάνημα-στόχος), (4) σφραγίδες/λογότυπα/
      watermarks στη σελίδα. **Spot-check εκκρεμεί** σε νέο πραγματικό import
      batch — δεν έχει δοκιμαστεί ακόμα ότι το tweak μειώνει πραγματικά το
      ποσοστό λάθους χωρίς να κάνει το machine_name υπερβολικά συχνά null σε
      γνήσιες περιπτώσεις (βλ. π.χ. ΤΡΕΙΛΟΡ, όπου ίδιο "Αρ. Αυτοκινήτου" πεδίο
      ήταν στην πραγματικότητα γνήσιο — η οδηγία (3) το αντιμετωπίζει σωστά ως
      "μην το χρησιμοποιήσεις παρά μόνο αν επιβεβαιώνεται από χειρόγραφο", όχι
      "ποτέ", αλλά αξίζει επιβεβαίωση σε πράξη).

## [invoicebook] Presence detection (MEGA/rclone)

Μεταφέρθηκε αυτούσιο από το προηγούμενο `invoices/TODO.md` — δες εκεί το
πλήρες ιστορικό/σχέδιο (δύο reference υλοποιήσεις: `expvault`,
`lab-galatista`). Προαπαιτούμενο: invoicebook δεν έχει καθόλου cloud
backup/sync υποδομή ακόμα.

## [invoicebook] Ξανα-σαρώσεις του ίδιου χαρτιού — dedup πάνω στο κείμενο (Layer 1β)

Το Layer 1 (ίδιο ΑΚΡΙΒΩΣ αρχείο, SHA256) υλοποιήθηκε 2026-09-23 (βλ. DONE.md) — **δεν πιάνει
ξανα-σαρώσεις** του ίδιου χαρτιού, αφού κάθε πέρασμα του σαρωτή παράγει διαφορετικά bytes
(θόρυβος εικόνας, metadata, compression). Μένει ο δεύτερος έλεγχος: hash πάνω στο εξαγόμενο
κείμενο (OCR/text layer), ανθεκτικό σε θόρυβο σάρωσης. Συνδέεται με το ήδη σχεδιασμένο
"Έλεγχος ορθότητας PDF" (pymupdf+Tesseract, βλ. παλιότερο πλήρες σχέδιο). Επίσης ανοιχτό: το
`pair_pdfs_by_date.py` δεν κάνει κανέναν έλεγχο (ο έλεγχος γίνεται μόνο στο confirm).

## [invoicebook] Retroactive bulk/machine tagging — ρητά αναβλήθηκε

159 migrated τιμολόγια καυσίμων χωρίς `bulk` flag/machine assignment. Δεν
προχωράει τώρα — μόνο αν αποδειχθεί ότι χρειάζεται.

## [invoicebook] Πίνακας Ελέγχου → πιθανή ενσωμάτωση στο Περιήγηση — μόνο ανάλυση, καμία υλοποίηση (2026-09-24)

Ο χρήστης ρώτησε αν το «Πίνακας Ελέγχου» (μηνιαία οικονομική σύνοψη, `get_summary`) θα
μπορούσε να ενσωματωθεί στο «Περιήγηση» όπως έγινε με το «Τιμολόγια» (βλ. DONE.md,
2026-09-24). **Συμπέρασμα: όχι με τον ίδιο τρόπο.** Το «Τιμολόγια» είχε ~90% κυριολεκτική
επικάλυψη λειτουργικότητας με το Περιήγηση· το Πίνακας Ελέγχου δουλεύει σε τελείως
διαφορετικό επίπεδο δεδομένων — άθροισμα header `net_amount`/`vat_amount`/`total_amount`
ανά **τιμολόγιο** (`get_summary` πάνω στο `tbl_invoices`, `GROUP BY` μήνα) — ενώ το
Περιήγηση δουλεύει ανά **γραμμή** (`get_invoice_items_by_category`, LEFT JOIN σε items,
ένα τιμολόγιο με Ν γραμμές εμφανίζεται Ν φορές). Δεν υπάρχει έτοιμο κοινό μοντέλο να
συγχωνευτεί άμεσα — καμία κωδική αλλαγή δεν έγινε, μόνο η ανάλυση παρακάτω, για μελλοντική
αναφορά αν ξαναανοίξει το θέμα.

**Ιδέα που συζητήθηκε**: κουμπί «Μηνιαία Αναφορά» στο Περιήγηση, πάνω στα ήδη φορτωμένα/
φιλτραρισμένα rows (τα οποία ήδη κουβαλούν flattened τα header amounts μέσω του LEFT JOIN —
άρα δεν θα χρειαζόταν νέο fetch).

**Κρίσιμο τεχνικό πρόβλημα που εντοπίστηκε**: επειδή κάθε τιμολόγιο εμφανίζεται μία φορά
ΑΝΑ ΓΡΑΜΜΗ, ένα naive `SUM(net_amount)` πάνω στα rows θα μετρούσε κάθε τιμολόγιο τόσες
φορές όσες γραμμές έχει (πολλαπλασιασμένο αποτέλεσμα, ΟΧΙ ίδιο με το `get_summary`). Λύση:
dedupe ανά `invoice_id` πριν το άθροισμα — απαραίτητη διόρθωση, όχι «ίδιο αποτέλεσμα, απλά
αλλάζουν οι στήλες» όπως αρχικά υποτέθηκε.

**Ανά φίλτρο, τι λύνεται και πώς:**
- **Χρονικό διάστημα** — ήδη δομημένο φίλτρο (`browse-date-from/to`), δουλεύει καθαρά και
  στις δύο λογικές άθροισης (header ή γραμμής).
- **Κατηγορία** — ήδη δομημένο (clickable stat cards), αλλά **αποκλείει** τη λογική «header
  amounts»: το net/vat/total του header είναι ΟΛΟΚΛΗΡΟΥ του τιμολογίου, δεν σπάει ανά
  κατηγορία γραμμών. Μόλις επιλεγεί κατηγορία, η αναφορά πρέπει να αλλάξει πηγή σε άθροισμα
  `value`/`quantity` γραμμών, όχι header amounts.
- **Προμηθευτής** — δεν υπάρχει σήμερα δομημένο φίλτρο (μόνο free-text substring search στο
  όνομα). ΑΛΛΑ το ΑΦΜ είναι πραγματικά μοναδικό (επιβεβαιώθηκε στη βάση 2026-09-24: 296
  προμηθευτές, 0 διπλότυπα ΑΦΜ, μόνο 12/296 χωρίς ΑΦΜ) — αρκεί η αναζήτηση να ταιριάζει με
  το ΑΦΜ μέσω της ήδη υπάρχουσας `_normalize_vat` λογικής (αγνοεί «EL»/κενά/παύλες) αντί για
  raw substring σε όνομα. Δεν χρειάζεται νέο dropdown. Fallback σε ακριβές (όχι substring)
  ταίριασμα ονόματος μόνο για το ~4% χωρίς ΑΦΜ.
- **Μηχάνημα** — δεν έχει δεύτερο μοναδικό αναγνωριστικό σαν το ΑΦΜ (ρητά τεκμηριωμένο ήδη
  στο section «Μηχανήματα» παρακάτω) — μόνο exact-normalized-name matching. Λύση: reuse του
  ήδη υπάρχοντος `attachAutocomplete`/`canonicalMachineName`/`AppState.machines` μηχανισμού
  (ίδιος με το edit-modal) ως φίλτρο δεσμευμένο σε `machine_id`, όχι νέα υποδομή. Όριο που
  ΔΕΝ λύνεται στο UI: μη-ακόμα-merged διπλότυπα `machine_id` για το ίδιο φυσικό μηχάνημα θα
  σπάνε τα αποτελέσματα σε πολλά κομμάτια — θέμα καθαριότητας δεδομένων (βλ. section
  «Μηχανήματα»), όχι του φίλτρου.

## [report-tool] "Μέση τιμή ανά τύπο προϊόντος" dashboard — παγωμένο

Σχεδιασμός έτοιμος (κατηγορίες προϊόντων Λιπαντικά, προσέγγιση
tagging με literal `[TAG]` prefix στο `description` αντί για query-time
regex). Τίποτα δεν έχει χτιστεί ακόμα. Όταν συνεχιστεί:
1. Επιβεβαίωση τελικής λίστας κατηγοριών.
2. Ποια χρόνια καλύπτει το "τελευταία χρόνια" (δεν έχει διευκρινιστεί).
3. Σχεδιασμός query/UI στο report-tool.

**Εύρημα 2026-09-24**: δεν χρειάζεται καθόλου tagging/`[TAG]` prefix — ομαδοποίηση των 877
γραμμών Λιπαντικά κατά τα πρώτα 1-2 tokens της περιγραφής (π.χ. `ISO 68`, `SAE 20W-50`,
`GREASE LI-3`) καλύπτει ήδη το 88% (769/877) μέσα από μόλις τα top 20 (από 76 distinct
prefixes συνολικά) — θα δούλευε live/query-time, χωρίς καμία αλλαγή/migration δεδομένων. Η
ουρά (36 singleton prefixes) είναι κυρίως μορφοποιητικές ασυνέπειες (π.χ. `SAE 10W30` vs
`SAE 10W-40`), ίδιο μοτίβο με την ήδη υπάρχουσα `_canonicalize_description`/
`normalizeMachineCode` κανονικοποίηση — με κανονικοποίηση παύλας/κενού το coverage θα
ανέβαινε ακόμα πιο πάνω. Παραμένει όμως reporting/analytics δουλειά → ανήκει στο
report-tool, όχι στο Περιήγηση. **Ρητή απόφαση χρήστη (2026-09-24): να μην αναλωθούμε άλλο
σε αυτό τώρα** — το εύρημα καταγράφεται μόνο για να μη χρειαστεί να ξαναβρεθεί από την αρχή.

## [invoicebook] Μηχανήματα — ό,τι μένει μετά το merge tool (2026-09-10)

`merge_machines(keep_id, merge_id)` + κάρτα στο tab «🧹 Καθαρισμός» υλοποιήθηκαν (βλ.
DONE.md) — αλλά μόνο exact-normalized-match detection (καμία δεύτερη ταυτοποίηση σαν
ΑΦΜ υπάρχει για μηχανήματα· ρητά τεκμηριωμένο στο ίδιο το UI, το χειροκίνητο merge-by-
id είναι ο κύριος τρόπος χρήσης). Από το αρχικό sweep του 2026-09-06 (182→75, βλ.
`[[project-galatista-machine-dedup-sweep]]`) μένουν ανοιχτά:
- [ ] **Χειρόγραφες σημειώσεις ανά γραμμή σε "γενικά" τιμολόγια (π.χ. MAN) κρύβουν το πραγματικό
      μηχάνημα** (NET9901-9904 κωδικοί) — χρειάστηκε πλήρες διάβασμα PDF ανά τιμολόγιο. Δεν υπάρχει
      τρόπος να αυτοματοποιηθεί χωρίς OCR σε επίπεδο γραμμής (βλ. ήδη σχεδιασμένο "Έλεγχος
      ορθότητας PDF" — θα μπορούσε να επεκταθεί να πιάνει και αυτό).
- [ ] Ξεχωριστό tier στο UI για "OCR letter-substitution" υποψήφιους — ίδιο αναβλήθηκε ρητά
      και στο supplier/description tool (χρειάζεται δικό του, πιο αργό, με-άνοιγμα-PDF βήμα).
      **2026-09-17**: το `ΕΦΥΡΙΑ`→`ΣΦΥΡΙΑ` και `ΟΛΗΝΤΥΡΙΟ`→`ΠΛΥΝΤΗΡΙΟ` merge (βλ. DONE.md) είναι
      ακριβώς αυτού του τύπου, έγιναν χειροκίνητα εν τω μεταξύ.
## [Cross-repo] 3 ξεχωριστά Electron windows

intake-tool / invoicebook / report-tool είναι 3 πλήρεις ανεξάρτητες
εφαρμογές. Θέληση να ενοποιηθούν σε λιγότερα παράθυρα — καμία δουλειά δεν
έχει ξεκινήσει, αρχιτεκτονική αλλαγή, χρειάζεται re-confirm scope πριν
ξεκινήσει. (Ξεχωριστό από την ήδη ολοκληρωμένη λειτουργική ενσωμάτωση
intake-tool→invoicebook, βλ. DONE.md 2026-09-23 — αυτό εδώ είναι για
παράθυρα/repos, όχι λειτουργικότητα/κώδικα.)

## [invoicebook] Ξεχωριστή κατηγορία μπαταριών + tracking επιστροφών λαδιών

Δύο σχετικές αλλά ξεχωριστές ιδέες, σημειώθηκαν μαζί (2026-08-27), καμία
δεν έχει σχεδιαστεί ακόμα σε βάθος:

- [ ] Μπαταρίες (`Μπαταρίες`) ως δική τους κατηγορία/bucket, ξεχωριστά από
      ό,τι αλλού καταλήγουν σήμερα — δεν υπάρχει καθόλου διακριτή κατηγορία
      στα σημερινά δεδομένα.
- [ ] Τρόπος να φαίνονται/καταγράφονται **επιστροφές** λαδιού σε εταιρείες
      ανακύκλωσης (παραλαβή χρησιμοποιημένου λαδιού) — διαφορετικό από την
      ήδη υπάρχουσα χρέωση "ΕΙΣΦΟΡΑ ΑΝΑΚΥΚΛΩΣΗΣ ΛΙΠΑΝΤΙΚΩΝ" (αυτή είναι
      χρέωση αγοράς, όχι outbound επιστροφή). Καμία δομή στο schema/workflow
      σήμερα δεν μοντελοποιεί εξερχόμενη συναλλαγή — νέο έδαφος, όχι
      τροποποίηση υπάρχοντος.

## [invoicebook] Κατηγορία «Διοικητικά/Λοιπά» — δημιουργήθηκε, όχι ακόμα universal

Σημειώθηκε 2026-09-13 κατά το description cleanup λιπαντικών, ίδια μέρα
**εκτελέστηκε προσωρινά** (χρήστης ζήτησε ρητά) μόνο για την κατηγορία
`Λιπαντικά`: δημιουργήθηκε νέα κατηγορία `Διοικητικά/Λοιπά` και μεταφέρθηκαν
εκεί 160 γραμμές — οι 155 της (πλέον ενοποιημένης) `Περιβαλλοντική εισφορά`
(Ν.2939/01) + οι 5 `ΠΑΛΕΤΕΣ` (που είχαν μόλις μεταφερθεί στο `Αναλώσιμα/
Γενικά`, μεταφέρθηκαν ξανά από εκεί στο νέο). Στόχος ήταν να "καθαρίσει" η
λίστα πραγματικών προϊόντων λιπαντικού (μέσος όρος τιμής/λίτρο κ.λπ.) από
γραμμές που δεν είναι προϊόντα. Backup πριν την αλλαγή:
`invoices/backend/invoicebook.db.bak-dioikitika-loipa-20260913_184633`.

- [ ] **Ρητά χαρακτηρισμένο ως "προσωρινό"** από τον χρήστη — δεν έχει
      επιβεβαιωθεί ότι μένει έτσι μόνιμα, ούτε αν επεκτείνεται στις άλλες
      κατηγορίες (καύσιμα, επισκευές κ.λπ., που έχουν δικές τους
      "Περιβαλλοντική εισφορά" γραμμές, ~144-155=~-11 ακόμα υπόλοιπες εκτός
      Λιπαντικών βάσει παλιότερου ελέγχου — δεν ξαναμετρήθηκε μετά). Re-
      confirm scope πριν το επεκτείνεις ή το θεωρήσεις οριστικό.

## [invoicebook] Καθαρισμός πίνακα tbl_import_staging — προαπαιτούμενο πλέον έτοιμο

Κατά τη διάρκεια του description cleanup λιπαντικών (2026-09-13, βλ. DONE.md)
διαπιστώθηκε ότι ο πίνακας
`tbl_import_staging` δεν έχει καμία γραμμή σε κατάσταση `pending` — όλες οι
~2.500+ γραμμές είναι ήδη `confirmed`/`merged`/`rejected`, δηλαδή ιστορικό
αρχείο του πώς μπήκε αρχικά κάθε εγγραφή, όχι ενεργή ουρά. Δεν εμφανίζεται
πουθενά στο UI ("Σε Αναμονή Επιβεβαίωσης" δείχνει μόνο status='pending', άρα
άδειο).

- [ ] **Το description cleanup πέρασμα (Λιπαντικά) έκλεισε 2026-09-13** —
      το προαπαιτούμενο για να προχωρήσει αυτό το καθάρισμα πλέον ισχύει.
      **Χρήστης δεν έχει ακόμα ζητήσει να εκτελεστεί** — μόνο να καταγραφεί
      ότι είναι πλέον έτοιμο· καθάρισε/άδειασε τον πίνακα (ή τουλάχιστον τις
      ήδη `confirmed`/`rejected`/`merged` γραμμές) μόνο όταν ζητηθεί ρητά.

## [Cross-repo] Μεταφορά projects σε εξωτερικό σκληρό δίσκο

Αναφέρθηκε μόνο σαν πρόθεση (2026-09-05), καμία λεπτομέρεια ακόμα: ποια
projects ακριβώς (intake-tool/invoicebook/report-tool/expvault — όλα;), ο
λόγος (backup, χώρος, φορητότητα;), αν αντικαθιστά ή προστίθεται στο ήδη
υπάρχον σχέδιο δύο-μηχανών/rclone sync. Να ζητηθεί scope όταν ξαναπιαστεί
το θέμα — μην υποτεθεί ότι είναι το ίδιο πράγμα με το two-machine setup
χωρίς επιβεβαίωση.

## [invoicebook] invoicebook — reconciliation

- [ ] **id 414**: το `total_amount` (176,26€) δεν συμφωνεί με net+ΦΠΑ
      (155,98€+23% θα έβγαινε 191,86€, διαφορά ~15,60€) — μικρό, όχι
      επείγον.
- [ ] **Λείπον τιμολόγιο ΠΑΠΑΝΤΩΝΙΟΥ** (~857,65€, μεταξύ 3-30/7/2020,
      εντοπισμένο μέσω αλυσίδας ΠΡΟΗΓ./ΝΕΟ ΥΠΟΛΟΙΠΟ, id 313↔328) —
      χρειάζεται εξωτερική πηγή (φυσικό χαρτί ή αντίγραφο από τον
      προμηθευτή), όχι κάτι που λύνεται μέσα από τη βάση.

## [intake-tool] Εργαλείο μαζικής τακτοποίησης ονομάτων/μονάδων

Σημειώθηκε 2026-09-21 κατά το πρώτο πραγματικό import εκρηκτικών
(NITROCHEM batch, 6 παραστατικά) — εντοπίστηκαν χειροκίνητα δύο ασυνέπειες
στα ήδη καταχωρημένα δεδομένα που χρειάστηκε να διορθωθούν με ad-hoc SQL
αντί για κάποιο εργαλείο του UI:
1. Μονάδα μέτρησης `TEM` (λατινικοί χαρακτήρες) vs `ΤΕΜ` (ελληνικοί) — οπτικά
   ίδια, τεχνικά διαφορετική τιμή, το ίδιο υλικό καταγραμμένο άλλοτε με το
   ένα άλλοτε με το άλλο μέσα στο ίδιο batch. Το `IMPORT_PROMPT` διορθώθηκε
   να το προλαβαίνει στο εξής (commit `7e3478c`), αλλά δεν καθαρίζει ό,τι
   είναι ήδη μέσα στη βάση από πριν.
2. `description` γραμμών που θα έπρεπε να ταιριάζουν ακριβώς με ήδη γνωστό
   canonical όνομα (π.χ. λίστα εκρηκτικών από expvault, βλ. `IMPORT_PROMPT`)
   αλλά δεν ταιριάζουν λόγω παλιότερης ελεύθερης καταχώρησης.

Το tab "Καθαρισμός/Συγχωνεύσεις" έχει ήδη μηχανισμό merge candidates για
περιγραφές/προμηθευτές/μηχανήματα, αλλά όχι για μονάδα μέτρησης, και η ροή
merge περιγραφών είναι ανά-ζευγάρι, όχι μαζική εφαρμογή ενός canonical
ονόματος/μονάδας σε πολλές γραμμές μονομιάς.

**Scope συμφωνήθηκε 2026-09-23 σε 2 βήματα.** Βήμα 1 (μονάδες) ΥΛΟΠΟΙΗΘΗΚΕ ίδια μέρα —
βλ. DONE.md. Βήμα 2 έγινε ξεχωριστό item παρακάτω.

## [intake-tool] Μαζική τακτοποίηση — Βήμα 2: περιγραφές προς λίστα σωστών ονομάτων

Συνέχεια του παραπάνω item (σημείο 2 εκεί). Για κατηγορία με γνωστή λίστα ονομάτων (π.χ.
Εκρηκτικά ↔ ονόματα expvault, ήδη στο `IMPORT_PROMPT`), κάρτα που δείχνει τις περιγραφές
ΕΚΤΟΣ λίστας και εφαρμόζει το επιλεγμένο σωστό όνομα σε όλες τις ίδιες γραμμές μαζί.

- [ ] **Αναβλήθηκε** μέχρι να ενωθούν τα δεδομένα — η βάση του coding PC δεν έχει ακόμα
      εκρηκτικά (είναι στο άλλο PC), δεν έχει τι να δείξει/δοκιμάσει.

